# Purpose:
#   Runs a cache warmup for the prod system
#
# Dependency:
#   EXT:typo3-warming
#
cache:prod:warmup:
  stage: test.prod
  extends:
    - .ssh
  needs:
    - build:php
    - deploy:prod
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "pipeline"
      when: never
    - if: $CI_COMMIT_TAG
      when: never
    - if: $CI_COMMIT_REF_NAME == "main"
      when: always
    - when: never
  variables:
    TYPO3_BASEURL: https://$DOMAIN_PROD/
  script:
    - !reference [.check-deployment-dependencies, script]
    - echo "\e[33mRunning cache warmup on $TYPO3_BASEURL\e[0m"
    - vendor/bin/dep cache:warmup prod -v
